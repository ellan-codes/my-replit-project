{
  "buildCommand": "cd client && npx vite build --outDir ../dist",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/booking", "destination": "/api/booking" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
